text = 'Python'
if text:
    print('Text is defined, is truthy') # same concept as truthynumber.py; don't need to check with equality to see it if it set/true
    # can do reverse logic ... if not text:
    # can do multiple checks ... if text and / or some other test
    # with multi tests remember and means both conditions must be true.  or means one or both must be true.